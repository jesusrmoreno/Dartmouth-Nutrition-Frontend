let meals = ['Breakfast', 'Dinner', 'Lunch'];


let DiaryEntry = {
  user: '',
  recipe: '',
  servingsMultiplier: 1,
};

// Create diaryEntry and if succesful add it to the existing user meal.
// Need to delete old user meals when they're empty...
function diaryEntry(recipe, servingsMultiplier) {

}
